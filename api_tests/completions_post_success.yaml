# Test generated by RoostGPT for test mistralai using AI Type Open AI and AI Model gpt-4


# Test generated for /chat/completions_post for http method type POST in artillery framework

# RoostTestHash=fc40a573a0


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "completions_post_success.csv"
    fields:
      - "model"
      - "messages"
      - "temperature"
      - "top_p"
      - "max_tokens"
      - "stream"
      - "safe_prompt"
      - "random_seed"
    skipHeader: true
scenarios:
  - name: "Chat Completions API Test"
    flow:
      - post:
          url: "/chat/completions"
          headers:
            content-type: "application/json"
            authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            model: "{{ model }}"
            messages: "{{ messages }}"
            temperature: "{{ temperature }}"
            top_p: "{{ top_p }}"
            max_tokens: "{{ max_tokens }}"
            stream: "{{ stream }}"
            safe_prompt: "{{ safe_prompt }}"
            random_seed: "{{ random_seed }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "object"
            - hasProperty: "created"
            - hasProperty: "model"
            - hasProperty: "choices"
            - hasProperty: "usage"
          think: 1
