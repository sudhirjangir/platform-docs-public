# ********RoostGPT********
# Test generated by RoostGPT for test git-public-api using AI Type Open AI and AI Model gpt-4

# Test generated for /chat/completions_post for http method type POST in artillery framework
# RoostTestHash=fc40a573a0



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "completions_post_others.csv"
    fields:
      - "model"
      - "messages"
      - "temperature"
      - "top_p"
      - "max_tokens"
      - "stream"
      - "safe_prompt"
      - "random_seed"
    skipHeader: true
  environments:
    dev:
      target: "https://api.example.com"

scenarios:
  - name: "Non-Success API Responses"
    flow:
      # Invalid payload
      - post:
          url: "/chat/completions"
          json:
            model: "mistral-tiny"
            messages: "InvalidMessage"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
          think: 1

      # Invalid authorization
      - post:
          url: "/chat/completions"
          json:
            model: "{{ model }}"
            messages: "{{ messages }}"
            temperature: "{{ temperature }}"
            top_p: "{{ top_p }}"
            max_tokens: "{{ max_tokens }}"
            stream: "{{ stream }}"
            safe_prompt: "{{ safe_prompt }}"
            random_seed: "{{ random_seed }}"
          headers:
            Authorization: "Bearer InvalidToken"
          expect:
            - statusCode: 401
          think: 1

      # Invalid URL
      - post:
          url: "/invalid/url"
          json:
            model: "{{ model }}"
            messages: "{{ messages }}"
            temperature: "{{ temperature }}"
            top_p: "{{ top_p }}"
            max_tokens: "{{ max_tokens }}"
            stream: "{{ stream }}"
            safe_prompt: "{{ safe_prompt }}"
            random_seed: "{{ random_seed }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 1
