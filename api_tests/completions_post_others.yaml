# Test generated by RoostGPT for test mistralai using AI Type Open AI and AI Model gpt-4


# Test generated for /chat/completions_post for http method type POST in artillery framework

# RoostTestHash=fc40a573a0


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "completions_post_others.csv"
    fields:
      - "model"
      - "messages"
      - "temperature"
      - "top_p"
      - "max_tokens"
      - "stream"
      - "safe_prompt"
      - "random_seed"
    skipHeader: true
  variables:
    AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "Non-Success API Responses"
    flow:
      # Testing the POST request with an incorrect URL
      - post:
          url: "/chatt/completions"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            model: "{{ model }}"
            messages: "{{ messages }}"
          expect:
            - statusCode: 404
          think: 1

      # Testing the POST request with an invalid payload
      - post:
          url: "/chat/completions"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            model: "{{ model }}"
            messages: "Invalid Payload"
          expect:
            - statusCode: 400
          think: 1

      # Testing the POST request with an invalid Authorization token
      - post:
          url: "/chat/completions"
          headers:
            Authorization: "Bearer InvalidToken"
            Content-Type: "application/json"
          json:
            model: "{{ model }}"
            messages: "{{ messages }}"
          expect:
            - statusCode: 401
          think: 1

      # Testing the POST request without required fields
      - post:
          url: "/chat/completions"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            model: "{{ model }}"
          expect:
            - statusCode: 400
          think: 1
